<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">  
<mapper namespace="com.sound.mall.model.dao.AdminDao">
    <insert id="insertAdmin" parameterType="com.sound.mall.model.dto.Admin">
        INSERT INTO ADMIN (user_id, authority)
        VALUES (#{userId}, #{authority})
    </insert>
</mapper>